# Log Analyzer

A command-line tool to parse web server log files and produce a summary report.
Handles multiple timestamp formats, missing fields, JSON lines, extra data, and malformed entries gracefully.

## Requirements
- Python 3.8+

## How to run
```bash
python log_analyzer.py <path_to_log_file>